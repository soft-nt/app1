---
phase: 06-layout-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/WeatherWidget.tsx
  - src/components/Header.tsx
  - src/App.tsx
  - src/App.css
autonomous: true

must_haves:
  truths:
    - "Date displays on left side of header"
    - "Weather placeholder displays on right side of header"
    - "Elements are horizontally arranged with flexbox"
    - "Geneva label is visible in weather placeholder"
    - "Layout maintains proper vertical alignment"
  artifacts:
    - path: "src/components/WeatherWidget.tsx"
      provides: "Weather placeholder component"
      exports: ["default"]
      contains: "Geneva"
    - path: "src/components/Header.tsx"
      provides: "Layout container with flexbox"
      exports: ["default"]
    - path: "src/App.css"
      provides: "Header flexbox styles"
      contains: ".header"
  key_links:
    - from: "src/components/Header.tsx"
      to: "src/components/DateDisplay.tsx"
      via: "import and render"
      pattern: "import DateDisplay"
    - from: "src/components/Header.tsx"
      to: "src/components/WeatherWidget.tsx"
      via: "import and render"
      pattern: "import WeatherWidget"
    - from: "src/App.tsx"
      to: "src/components/Header.tsx"
      via: "import and render with color prop"
      pattern: "import Header"
    - from: "src/App.css"
      to: "Header component"
      via: "className header"
      pattern: "display:\\s*flex"
---

<objective>
Create header layout with date on left and weather placeholder on right using CSS Flexbox.

Purpose: Establish visual structure for weather widget integration in Phase 7
Output: Working header component with flexbox layout, weather placeholder showing "Geneva"
</objective>

<execution_context>
@/home/ntorrent/.claude/get-shit-done/workflows/execute-plan.md
@/home/ntorrent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-layout-integration/06-RESEARCH.md

# Source files to modify
@src/App.tsx
@src/App.css
@src/components/DateDisplay.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WeatherWidget placeholder component</name>
  <files>src/components/WeatherWidget.tsx</files>
  <action>
Create WeatherWidget.tsx as a placeholder component for Phase 7.

Component requirements:
- Functional component with no props (placeholder state)
- Display "Geneva" as location label
- Use className "weather-widget" for styling
- Export as default

Structure:
```tsx
function WeatherWidget() {
  return (
    <div className="weather-widget">
      <span className="weather-location">Geneva</span>
    </div>
  );
}
```

Do NOT add loading states, API integration, or temperature display - that's Phase 7.
  </action>
  <verify>File exists at src/components/WeatherWidget.tsx with "Geneva" text and default export</verify>
  <done>WeatherWidget.tsx created with Geneva label and weather-widget className</done>
</task>

<task type="auto">
  <name>Task 2: Create Header component and integrate in App</name>
  <files>src/components/Header.tsx, src/App.tsx, src/App.css</files>
  <action>
**Header.tsx:**
Create Header component that composes DateDisplay and WeatherWidget with flexbox layout.

- Accept `color` prop (string) to pass to DateDisplay
- Use semantic `<header>` element with className "header"
- Render DateDisplay (left) and WeatherWidget (right)
- Export as default

```tsx
type HeaderProps = {
  color: string;
};

function Header({ color }: HeaderProps) {
  return (
    <header className="header">
      <DateDisplay color={color} />
      <WeatherWidget />
    </header>
  );
}
```

**App.tsx:**
Update to use Header instead of directly rendering DateDisplay.

- Import Header from './components/Header'
- Remove direct DateDisplay import
- Replace `<DateDisplay color={color} />` with `<Header color={color} />`
- Keep Button below Header

**App.css:**
Add flexbox styles for header layout.

```css
.header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 2rem;
  padding: 1rem 0;
}
```

Update .date-display to remove text-align: center (parent flexbox handles positioning).

Add weather-widget styles:
```css
.weather-widget {
  display: flex;
  align-items: center;
}

.weather-location {
  font-size: 1rem;
  font-weight: 500;
}
```
  </action>
  <verify>
Run `npm run dev` and visually confirm:
1. Date appears on left side of page
2. "Geneva" text appears on right side
3. Both are horizontally aligned on same row
4. Button remains centered below header
  </verify>
  <done>
Header component created, App.tsx updated to use Header, CSS flexbox layout working with date left and Geneva right
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npm run build` succeeds without TypeScript errors
2. `npm run dev` shows date on left, "Geneva" on right
3. Button still changes date color when clicked
4. Layout maintains alignment at different viewport widths
</verification>

<success_criteria>
- [ ] LAY-01: Header displays date on left side
- [ ] LAY-02: Header displays weather widget on right side
- [ ] LAY-03: Header uses flexbox for horizontal arrangement
- [ ] LAY-04: Header elements have appropriate spacing (gap: 2rem)
- [ ] WTH-04: Location label "Geneva" displays
- [ ] Existing color change functionality preserved
</success_criteria>

<output>
After completion, create `.planning/phases/06-layout-integration/06-01-SUMMARY.md`
</output>
